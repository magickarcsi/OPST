<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Tracker</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      <% if person_signed_in? %>
        <li class="active"><a href="#">UK-<%= current_person.store %> <span class="sr-only">(current)</span></a></li>
      <% if current_person.try(:admin?) %>
        <li><%= link_to "Setup", people_path %></li>
      <% end %> 
        <li><%= link_to "People", people_path %></li>
        <li><%= link_to "DTS Hourlies", dts_hourlies_path %></li>
      <% end %>  
      </ul>
      
      <ul class="nav navbar-nav navbar-right">
        <% if person_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= "#{current_person.firstname} #{current_person.lastname}" %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            
                <li><%= link_to "Profile", person_path(current_person)%></a></li>
                <li><a href="#">Store: <%= current_person.store %></a></li>
                <li><a href="#">Settings</a></li>
                <li role="separator" class="divider"></li>
                <li>
                <%= link_to('Logout', destroy_person_session_path, method: :delete) %>        
                </li>
                <% else %>
                <li>
                <%= link_to('Login', new_person_session_path)  %>  
                </li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>